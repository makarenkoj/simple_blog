<%= turbo_frame_tag "follow_button_#{user.id}" do %>
  <% if current_user.following?(user) %>
    <%= button_to unfollow_user_path(user), method: :delete, title: t('follows.unfollow'), class: "inline-flex items-center px-6 py-2 border border-slate-300 rounded-full text-sm font-medium text-slate-800 hover:bg-slate-50" do %>
      <i class='bx bx-check mr-2'></i>
      <%= t('follows.unfollow') %>
    <% end %>
  <% else %>
    <%= button_to follow_user_path(user), title: t('follows.follow'), class: "inline-flex items-center px-6 py-2 bg-slate-900 rounded-full text-sm font-medium hover:bg-slate-800" do %>
      <i class='bx bx-plus mr-2'></i>
      <%= t('activerecord.attributes.user.follow') %>
    <% end %>
  <% end %>
<% end %>
