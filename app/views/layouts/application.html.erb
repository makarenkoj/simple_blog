<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Super Blog" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps %>
    <%= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%= stylesheet_link_tag :app, "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <link href="https://cdn.jsdelivr.net/npm/remixicon@4.2.0/fonts/remixicon.css" rel="stylesheet">
    <link href='https://unpkg.com/boxicons@2.1.4/css/boxicons.min.css' rel='stylesheet'>
  </head>
  <body class="bg-white" data-controller="sidebar">
    <nav class="sticky top-0 z-50 bg-white border-b border-slate-200">
      <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
        <div class="flex items-center justify-between h-16">
          <div class="flex items-center space-x-4">
            <button type="button" class="lg:hidden text-slate-700 hover:text-slate-900 transition-colors duration-200" data-action="click->sidebar#toggleSidebar">
              <i class="ri-menu-line text-2xl"></i>
            </button>
            <%= link_to root_path, class: "flex items-center space-x-2 group" do %>
              <div class="w-8 h-8 bg-slate-900 rounded-lg flex items-center justify-center">
                <i class='bx bxl-blogger text-white text-xl'></i>
              </div>
              <span class="hidden sm:!block text-xl font-serif font-bold text-slate-900">
                <%= t('activerecord.app.simple', default: 'Simple')%> <%= t('activerecord.app.blog', default: 'Blog')%>
              </span>
            <% end %>
          </div>
          <!-- Center: Search (Desktop) -->
          <div class="hidden lg:!block flex-1 max-w-md mx-8">
            <div class="relative">
              <input 
                type="text" 
                placeholder="<%= t('search', default: 'Пошук...') %>"
                class="w-full px-4 py-2 pl-10 bg-slate-100 border border-transparent rounded-full text-sm focus:outline-none focus:bg-white focus:border-slate-300 transition-all duration-200"
              >
              <i class="ri-search-line absolute left-3 top-1/2 -translate-y-1/2 mt-3.5 text-slate-400 text-lg"></i>
            </div>
          </div>
          <!-- Right: Actions -->
          <div class="flex items-center space-x-2 sm:space-x-3">
            <% if user_signed_in? %>
              <!-- Search Icon (Mobile only) -->
              <button type="button" class="lg:hidden p-2 text-slate-600 hover:text-slate-900 transition-colors duration-200">
                <i class="ri-search-line text-xl"></i>
              </button>
              <%= link_to '#', class: "hidden lg:!inline-block p-2 text-slate-600 hover:text-slate-900 transition-colors duration-200" do %>
                <span class="text-sm font-medium"><%= t('activerecord.attributes.posts.library') %></span>
              <% end %>
              <div class="dropdown" data-controller="dropdown">
                <button type="button" class="relative p-2 text-slate-600 hover:text-slate-900 transition-colors duration-200" data-action="click->dropdown#toggle" data-dropdown-target="toggle">
                  <i class="ri-notification-3-line text-xl"></i>
                  <span class="absolute top-1.5 right-1.5 w-2 h-2 bg-red-500 rounded-full"></span>
                </button>
                <div class="dropdown-menu shadow-xl border border-slate-200 z-30 dropdown-hidden w-80 bg-white rounded-lg overflow-hidden" data-dropdown-target="menu">
                  <div class="p-4 border-b border-slate-200">
                    <h3 class="text-sm font-bold text-slate-900">
                      <%= t('notifications', default: 'Сповіщення') %>
                    </h3>
                  </div>
                  <ul class="max-h-96 overflow-y-auto custom-scrollbar">
                    <% 5.times do |i| %>
                      <li>
                        <a href="#" class="block px-4 py-3 hover:bg-slate-50 transition-colors duration-200">
                          <div class="flex items-start space-x-3">
                            <div class="w-10 h-10 rounded-full bg-slate-900 flex items-center justify-center text-white font-semibold flex-shrink-0">
                              U
                            </div>
                            <div class="flex-1 min-w-0">
                              <p class="text-sm text-slate-900 font-medium">
                                <%= t('notification_title', default: 'Нове повідомлення') %>
                              </p>
                              <p class="text-xs text-slate-500 mt-0.5">
                                <%= t('notification_time', default: '2 години тому') %>
                              </p>
                            </div>
                          </div>
                        </a>
                      </li>
                    <% end %>
                  </ul>
                </div>
              </div>
              <!-- User Profile -->
              <div class="dropdown" data-controller="dropdown">
                <button type="button" class="flex items-center space-x-2 p-1 hover:bg-slate-100 rounded-full transition-colors duration-200" data-action="click->dropdown#toggle" data-dropdown-target="toggle">
                  <% if current_user.avatar.attached? %>
                    <%= image_tag current_user.avatar.variant(resize_to_limit: [32, 32]), class: "w-8 h-8 rounded-full object-cover" %>
                  <% else %>
                    <div class="w-8 h-8 rounded-full bg-slate-900 flex items-center justify-center text-white text-sm font-semibold">
                      <%= current_user.full_name&.first&.upcase || '?' %>
                    </div>
                  <% end %>
                </button>
                <ul class="dropdown-menu shadow-xl border border-slate-200 z-30 dropdown-hidden py-2 rounded-lg bg-white w-56" data-dropdown-target="menu">
                  <li class="px-4 py-3 border-b border-slate-200">
                    <p class="text-sm font-semibold text-slate-900 truncate">
                      <%= current_user.full_name %>
                    </p>
                    <p class="text-xs text-slate-500 truncate">
                      <%= current_user.email %>
                    </p>
                  </li>
                  <li>
                    <%= link_to user_path(current_user), class: "flex items-center space-x-3 px-4 py-2.5 text-sm text-slate-700 hover:bg-slate-50 transition-colors duration-200" do %>
                      <i class='bx bx-user text-lg'></i>
                      <span><%= t('activerecord.attributes.user.profile') %></span>
                    <% end %>
                  </li>
                  <% if current_user.creator? %>
                    <li>
                      <%= link_to new_post_path, class: "flex items-center space-x-3 px-4 py-3 text-slate-700 hover:bg-slate-100 rounded-lg transition-colors duration-200" do %>
                        <i class='bx bx-edit text-xl'></i>
                        <span class="text-sm font-medium"><%= t('activerecord.attributes.posts.new') %></span>
                      <% end %>
                    </li>
                    <li>
                      <%= link_to posts_path, class: "flex items-center space-x-3 px-4 py-3 text-slate-700 hover:bg-slate-100 rounded-lg transition-colors duration-200" do %>
                        <i class='bx bx-collection text-xl'></i>
                        <span class="text-sm font-medium"><%= t('activerecord.attributes.posts.creators') %></span>
                      <% end %>
                    </li>
                  <% end %>
                  <li class="border-t border-slate-200 mt-2 pt-2">
                    <%= link_to destroy_user_session_path, data: { turbo_method: :delete }, class: "flex items-center space-x-3 px-4 py-2.5 text-sm text-slate-700 hover:bg-slate-50 transition-colors duration-200" do %>
                      <i class='bx bx-log-out text-lg'></i>
                      <span><%= t('activerecord.attributes.user.sign_out', default: 'Вийти') %></span>
                    <% end %>
                  </li>
                </ul>
              </div>
            <% else %>
              <!-- Guest Actions -->
              <%= link_to new_user_session_path, class: "px-4 py-2 text-sm font-medium text-slate-700 hover:text-slate-900 transition-colors duration-200" do %>
                <%= t('activerecord.attributes.user.sign_in', default: 'Увійти') %>
              <% end %>
              <%= link_to new_user_registration_path, class: "px-4 py-2 bg-slate-900 text-white text-sm font-medium rounded-full hover:bg-slate-800 transition-colors duration-200" do %>
                <%= t('get_started', default: 'Почати') %>
              <% end %>
            <% end %>
          </div>
        </div>
      </div>
    </nav>
    <!-- Mobile Sidebar Drawer -->
    <aside class="fixed left-0 top-0 w-72 h-full bg-white shadow-2xl border-r border-slate-200 z-50 sidebar-menu transition-transform duration-300 -translate-x-full" data-sidebar-target="sidebarMenu">
      <div class="p-6 border-b border-slate-200">
        <div class="flex items-center justify-between">
          <h2 class="text-lg font-serif font-bold text-slate-900">
            <%= t('menu', default: 'Меню') %>
          </h2>
          <button type="button" class="text-slate-400 hover:text-slate-900" data-action="click->sidebar#hideSidebar">
            <i class="ri-close-line text-2xl"></i>
          </button>
        </div>
      </div>
      <nav class="p-4 space-y-1 overflow-y-auto max-h-[calc(100vh-100px)] custom-scrollbar">
        <% if user_signed_in? %>
          <%= link_to posts_path, class: "flex items-center space-x-3 px-4 py-3 text-slate-700 hover:bg-slate-100 rounded-lg transition-colors duration-200" do %>
            <i class='bx bx-home text-xl'></i>
            <span class="text-sm font-medium"><%= t('home', default: 'Головна') %></span>
          <% end %>
          <%= link_to '#', class: "flex items-center space-x-3 px-4 py-3 text-slate-700 hover:bg-slate-100 rounded-lg transition-colors duration-200" do %>
            <i class='bx bx-library text-xl'></i>
            <span class="text-sm font-medium"><%= t('activerecord.attributes.posts.library') %></span>
          <% end %>
          <div class="border-t border-slate-200 my-4"></div>
        <% end %>
      </nav>
    </aside>
    <div class="fixed inset-0 bg-black/50 z-40 dropdown-hidden sidebar-overlay transition-opacity duration-300" data-sidebar-target="sidebarOverlay" data-action="click->sidebar#hideSidebar"></div>
    <!-- Main Content -->
    <main class="min-h-screen" data-sidebar-target="mainContent">
      <!-- Flash Messages -->
      <% if notice %>
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-6">
          <div class="bg-green-50 border-l-4 border-green-500 p-4 rounded-r-lg animate-slideDown">
            <div class="flex items-start">
              <i class='bx bx-check-circle text-green-500 text-xl flex-shrink-0 mt-0.5'></i>
              <p class="ml-3 text-sm text-green-800"><%= notice %></p>
              <button type="button" class="ml-auto text-green-500 hover:text-green-700" onclick="this.parentElement.parentElement.remove()">
                <i class='bx bx-x text-xl'></i>
              </button>
            </div>
          </div>
        </div>
      <% end %>
      <% if alert %>
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 pt-6">
          <div class="bg-red-50 border-l-4 border-red-500 p-4 rounded-r-lg animate-slideDown">
            <div class="flex items-start">
              <i class='bx bx-error-circle text-red-500 text-xl flex-shrink-0 mt-0.5'></i>
              <p class="ml-3 text-sm text-red-800"><%= alert %></p>
              <button type="button" class="ml-auto text-red-500 hover:text-red-700" onclick="this.parentElement.parentElement.remove()">
                <i class='bx bx-x text-xl'></i>
              </button>
            </div>
          </div>
        </div>
      <% end %>
      <%= yield %>
    </main>
    <!-- Mobile Write FAB (hidden on desktop) -->
    <% if user_signed_in? && current_user.creator? %>
      <%= link_to new_post_path, class: "lg:hidden fixed bottom-6 right-6 z-30 w-14 h-14 bg-slate-900 text-white rounded-full shadow-xl flex items-center justify-center hover:bg-slate-800 transition-colors duration-200" do %>
        <i class='bx bx-edit text-2xl'></i>
      <% end %>
    <% end %>
  </body>
</html>
